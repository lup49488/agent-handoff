Implement `slugify(title)` in `slug.py` for display titles. It must return a
lowercase ASCII slug, collapse separators, transliterate accented Latin text,
expand `&` as `and`, and expand `#` as `sharp`. Raise `TypeError` for a
non-string input. Keep the public function name unchanged and make
`python acceptance.py` pass.
